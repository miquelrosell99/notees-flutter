<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
      Release configuration: cleartext HTTP is denied by default and only
      allowed for common private / self-hosted hostnames where users commonly
      run Notees without a TLS certificate.

      This scopes cleartext as tightly as Android's static Network Security
      Config allows. Exact IP ranges (e.g. all 192.168.x.x addresses) cannot be
      expressed in NSC, so HTTP on arbitrary LAN IPs requires either HTTPS,
      a private DNS name covered below, or a debug build.
    -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">::1</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">local</domain>
        <domain includeSubdomains="true">ts.net</domain>
        <domain includeSubdomains="true">lan</domain>
        <domain includeSubdomains="true">home.arpa</domain>
        <domain includeSubdomains="true">internal</domain>
        <domain includeSubdomains="true">home</domain>
        <domain includeSubdomains="true">loc</domain>
    </domain-config>
</network-security-config>
