<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base app theme (light, edge-to-edge) -->
    <style name="Theme.Notees" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/on_surface</item>
        <item name="colorError">@color/error</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>

    <!-- Setup screen theme -->
    <style name="Theme.Notees.Setup" parent="Theme.Notees">
        <item name="android:statusBarColor">@color/setup_background</item>
    </style>

    <!-- Transparent theme for trampoline activities (e.g. ShareActivity) -->
    <style name="Theme.Notees.Transparent" parent="Theme.Notees">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

</resources>
