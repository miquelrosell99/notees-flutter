gradle-wrapper.jar
/.gradle
/.kotlin
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
# The debug keystore is intentionally tracked so CI and local builds share the
# same signing certificate and consecutive APK installs do not conflict.
!app/debug.keystore
