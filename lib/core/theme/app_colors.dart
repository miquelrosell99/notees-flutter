/// Fleet design-system colors for Notees mobile.
///
/// Layer 1 is a monochrome base (the default). Layer 2 is an optional
/// functional accent (sage green or muted orange). Layer 3 is dynamic color,
/// supplied at runtime by the user's device.
library;

import 'package:flutter/material.dart';

/// Functional accent for Notees: calm sage green.
const Color noteesAccent = Color(0xFF5B7D5B);

/// Alternative functional accent: muted terracotta orange.
const Color noteesAccentOrange = Color(0xFFB0763D);
